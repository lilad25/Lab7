services:
  - type: web
    name: lab7-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist/Lab7
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
